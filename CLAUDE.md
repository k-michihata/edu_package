# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際のガイドです。

## プロジェクト概要

将来可能性教育を支援するWebツール。
高校生がAIの生成した将来の事象（ライフイベント・社会変化）に対してポジ/ネガ評価と行動記述を行い、結果を「人生グラフ」として可視化する。

- **フル版仕様書：`full/` 配下を必ず最初に読むこと**（01_overview 〜 07_tech）
- MVP仕様書：`mvp/00_mvp.md`（実装済み。中核フローの仕様として引き続き有効）

## ブランチ運用

- `main`：フル版の開発を進めるブランチ
- `mvp` ブランチ / `mvp-v1` タグ：MVP完成時点のスナップショット。**凍結。変更しない**（デモ用デプロイの参照元）

## 開発スコープ（フル版）

MVPで実装済みの中核フロー（AI事象生成 → ポジ/ネガ評価 → 行動記述 → 人生グラフ）を土台に、以下を段階的に追加する。詳細は `full/04_requirements.md` を参照。

- 自己分析フェーズ（価値観記述・キャリアアンカー診断）
- 生徒による事象入力（グループブレインストーミング）
- ログイン・認証とデータの永続保存
- 教師向けアドミン機能（進捗管理・人生グラフ一覧・問いかけカスタマイズ）

## 技術スタック

| 項目 | 技術 |
|------|------|
| フレームワーク | Next.js（App Router）+ TypeScript |
| ホスティング | Vercel |
| AIモデル | OpenAI API `gpt-5-mini` |
| グラフ描画 | Recharts |
| DB・認証 | Supabase（PostgreSQL + Auth。当面は匿名サインイン、Google OAuthは将来追加） |
| データ保持 | Supabase にユーザー単位で永続保存 |
| スタイリング | Tailwind CSS |

## Supabase の方針

- スキーマは `supabase/schema.sql` で管理（変更時はこのファイルを更新する）
- クライアントは `src/lib/supabase/`（`client.ts`＝ブラウザ、`server.ts`＝サーバー）を使う
- **RLSを必ず有効にする**。生徒のデータは本人のみ読み書き可能（教師の閲覧ポリシーはアドミン実装時に追加）
- APIルート・サーバーコンポーネントは anon キー + ユーザーセッションでアクセスし、RLSに保護を委ねる。service role キーは原則使わない
- 認証保護は `src/middleware.ts`（画面はリダイレクト、APIは各ハンドラで401）
- 認証は当面 **匿名サインイン**（トップの「始める」で自動作成。ログインUIなし）。学校アカウント（Google OAuth）はロール管理（F-01/F-03）実装時に追加する

## アーキテクチャ方針

- AI呼び出しは必ず **Next.js API Routes（サーバーサイド）経由** で行う。APIキーをクライアントに露出させない
- AIプロバイダは抽象化する（`lib/ai/` に client を置き、OpenAI → Anthropic 等へ差し替え可能にする）
- AIの応答はJSON形式で受け取り、パース失敗時のフォールバックを実装する
- 状態管理はReactの標準機能（useState / useContext）で十分。外部ライブラリは導入しない

## AI事象生成の仕様

**入力：** 対象年代（前半 10〜40代 / 後半 50〜80代）

**プロンプト要件：**
- 日本の高校生を主語とした、平均的なライフイベント・社会変化を生成
- ポジティブ・ネガティブな事象を混在させる
- 社会変化とライフイベントを混在させる
- 対象年代内で年齢がばらけるようにする
- 出力はJSONのみ

**出力形式：**
```json
[
  { "age_at_event": 22, "description": "大学を卒業し、就職活動を経て社会人になる" }
]
```

## 環境変数

```
OPENAI_API_KEY=  # .env.local に置く。コミットしない
```

## 言語・UI

- UIの表示言語は**日本語**
- 対象ユーザーは日本の高校生。難しい言葉を避け、直感的に操作できるUIにする
- レスポンシブ対応（学校のPC・タブレット・Chromebookを想定）

## コーディング規約

- TypeScript strict モード
- コンポーネントは機能単位で `components/` に分割
- コメント・変数名の説明はコードの意図が伝わる最小限に
